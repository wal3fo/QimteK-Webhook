# ============================================
# QimteK Hooks - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ============================================
# Environment Mode
# ============================================
# Set to 'production' for production, 'development' for local dev
NODE_ENV=development

# ============================================
# Server Configuration
# ============================================
# Port for local development server
PORT=3001

# Base URL for webhook generation (used in webhook URLs)
# For local: http://localhost:3001
# For production: https://your-domain.vercel.app
BASE_URL=http://localhost:3001

# Client URL for CORS and Socket.io connections
# For local: http://localhost:5173
# For production: https://your-domain.vercel.app
CLIENT_URL=http://localhost:5173

# ============================================
# Database Configuration (Supabase)
# ============================================
# REQUIRED for Backend Functions (Cloudflare Pages)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional: Anon key if needed by client, though backend uses Service Role
SUPABASE_ANON_KEY=your-anon-key

# Direct Postgres connection (for migrations: npm run db:migrate)
# Supabase Dashboard > Project Settings > Database > Connection string (URI)
# DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# JWT Secret for signing tokens
JWT_SECRET=your-secret-key-change-in-production

# ============================================
# Database Configuration (Local Development - Legacy)
# ============================================
# Path to SQLite database file (only used locally)
# Default: ./webhook.db
DB_PATH=./webhook.db

# ============================================
# Frontend Configuration (Vite)
# ============================================
# API URL for frontend requests
# For local: http://localhost:3001/api
# For production: /api (relative path)
VITE_API_URL=http://localhost:3001/api

# ============================================
# Site Configuration
# ============================================
VITE_SITE_URL=https://qimte-k-webhook--incengine.replit.app
VITE_SITE_IMAGE=https://qimte-k-webhook--incengine.replit.app/dtg9whmIOP.webp
VITE_SITE_LOGO=https://qimte-k-webhook--incengine.replit.app/QimteKw.png
VITE_COMPANY_URL=https://qimtek.ma
VITE_GITHUB_URL=https://github.com/wal3fo
VITE_PAYPAL_URL=https://www.paypal.com/paypalme/drgineer/5?currencyCode=USD
VITE_PAYPAL_ME_URL=https://www.paypal.com/paypalme/drgineer
VITE_GA_ID=G-XE4C8MDJKS
VITE_COOKIEBOT_ID=647871cb-3b93-4d64-b47a-4d35e943a9ff
VITE_PAYPAL_BUTTON_ID=YATY56ANEDQYJ

# PayPal Configuration (For Payments)
PAYPAL_CLIENT_ID=your_paypal_client_id
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_secret

# ============================================
# Notes
# ============================================
# 1. For LOCAL DEVELOPMENT:
#    - Set NODE_ENV=development
#    - Use localhost URLs for BASE_URL, CLIENT_URL, VITE_API_URL
#
# 2. For VERCEL PRODUCTION:
#    - Set NODE_ENV=production (or Vercel sets it automatically)
#    - Set BASE_URL and CLIENT_URL to your Vercel domain
#    - Set VITE_API_URL=/api (relative path)
#
# 3. Environment variables prefixed with VITE_ are exposed to the frontend
#    All other variables are server-side only
VITE_ADSENSE_ID=
